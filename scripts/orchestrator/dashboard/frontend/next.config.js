// Autopilot Dashboard Next.js Config
// Copyright (c) 2026 Jeremy McSpadden <jeremy@fluxlabs.net>

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: 'http://localhost:8000/api/:path*',
      },
    ];
  },
};

module.exports = nextConfig;
